const disDir = `./dist/`;
const cacheDir = `./.cache/`;
// const distFile = (item) => `${disDir}${item}`;
// const wpFile = (item) => `${themeDir}dist/${item}`;


// module.exports = { disDir, cacheDir, distFile, wpFile };
module.exports = { disDir, cacheDir };